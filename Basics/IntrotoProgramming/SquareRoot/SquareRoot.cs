using System;

    class SquareRoot
    {
        static void Main()
        {
            int Num = 12345;
            double SqrtNum = Math.Sqrt(Num);
            Console.WriteLine("The square root of {0} is {1}", Num, SqrtNum);
        }
    }

